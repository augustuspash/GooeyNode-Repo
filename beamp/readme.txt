Download instructions for beamp are here: https://github.com/wayspurrchen/beamp

Then modify config.json to point to it's location (replace "<location here>" with location, 
if the location has spaces in the path then put \" around the path, the \ in the path need to be \\)